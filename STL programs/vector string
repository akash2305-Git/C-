#include<vector>
#include<string>
using namespace std;

//Complete this method, don't write main
vector<string> fizzbuzz(int n){
    
    for(int i=0;i<n;i++){
        if(n==3 && n == 5){
           string.pushback("FizzBuzz")
        }
        else if(n==3){
            string.pushback("Fizz")
        }
        else if(n==5){
            string.pushback("Buzz")
        }
    }
    
    
    
    
    
    
    
    
    
}